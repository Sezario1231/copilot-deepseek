<Window x:Class="deepseek_copilot.SidebarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:deepseek_copilot"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="450">
    <Grid>
        <Border x:Name="MainBorder" CornerRadius="8,0,0,8" Background="#FFFFFF">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" Opacity="0.25" ShadowDepth="2" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Title Bar -->
                <Border x:Name="TitleBar" Grid.Row="0" Background="#FFFFFF" CornerRadius="8,0,0,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                            <Image x:Name="TitleIcon" Width="18" Height="18"/>
                            <TextBlock x:Name="TitleText" Text="加载中…" FontSize="14" FontWeight="SemiBold"
                                       Margin="8,0,0,0"
                                       Foreground="#1A1A2E"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0">
                            <Button x:Name="SettingsButton" Content="⚙" Width="32" Height="32"
                                    Background="Transparent" BorderThickness="0"
                                    Foreground="#666" FontSize="16"
                                    Click="OnSettingsClick" Cursor="Hand"/>
                            <Button x:Name="CloseButton" Content="✕" Width="32" Height="32"
                                    Background="Transparent" BorderThickness="0"
                                    Foreground="#666" FontSize="16"
                                    Click="OnCloseClick" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- WebView Container -->
                <Grid Grid.Row="1" x:Name="WebViewHost"/>
            </Grid>
        </Border>
    </Grid>
</Window>
